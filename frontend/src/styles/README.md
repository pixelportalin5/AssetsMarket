# Styles

- `globals.css` — Tailwind + imports from `@assetsmarket/ui/styles`
- `theme.css` — CSS variables (dark glass tokens)

Design system organization: primitives in `packages/ui`, app theme overrides here.
