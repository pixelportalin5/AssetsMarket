# AssetsMarket environment setup
#
# This monorepo uses two env files only:
#   backend/.env          — database, auth, server (see backend/.env.example)
#   frontend/.env.local   — public browser config (see frontend/.env.example)
#
# Prisma commands load backend/.env automatically — do NOT create packages/database/.env
