# Configuration

Zod-validated environment variables — app exits on invalid config.

## Modules (planned)

- `env.ts` — all variables
- `app.ts` — port, API prefix
- `auth.ts` — JWT settings
- `redis.ts`, `aws.ts`

Copy root `.env.example` to `backend/.env` when implementing.
