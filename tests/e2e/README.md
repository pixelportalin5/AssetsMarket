# E2E tests

Playwright at repo root — exercises `frontend` + `backend` together.

Critical paths: auth, browse asset, seller listing draft, advertiser booking flow.

Base URL: `http://localhost:3000` with API on `:4000`.
