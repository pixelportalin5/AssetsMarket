# UI styles

- `globals.css` — base layer imports for apps
- `tailwind.preset.ts` — shared Tailwind theme extension

Apps import:

```css
@import "@assetsmarket/ui/styles/globals.css";
```

Tokens synchronized with `frontend/src/styles/theme.css` — UI package owns canonical values.
