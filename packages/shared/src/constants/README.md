# Constants

- `ROLES` — buyer, seller, advertiser, moderator, admin
- `ASSET_CATEGORIES` — website, domain, mobile_app, etc.
- `ERROR_CODES` — machine-readable API errors
- `BOOKING_STATUS`, `TRANSACTION_STATUS` — mirror DB enums when schema exists

Single source for labels and API codes.
