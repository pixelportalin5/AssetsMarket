# Shared types

TypeScript interfaces used by both web and API:

- `PaginatedResponse<T>`
- `ApiErrorResponse`
- Domain view models (e.g. `AssetSummary`, `BookingSummary`)

**Not** Prisma generated types — map in API repositories to these DTOs.
